Q1：a0: 一开始放printf中字符串的地址， 后来放exit的值0；a1: 12；a2:13； sp:栈寄存器；ra:返回地址；s0:中间值
Q2：f: 编译器觉得f(8)+1太简单了，在编译时就执行了该函数的结果为12（内联），简化为了li	a1,12一条指令。
	g: 编译器觉得g()逻辑也太简单了，在f中直接内联了g的逻辑（x+3),在f直接进行+3操作
Q3：6f6
Q4：返回地址为34
Q5：He110 Worlds 大端：i = 0x726c6400 57616不需要换
Q6：printf报错：少了参数。事实上，printf会从a2寄存器取值，但由于a2寄存器值未初始化，所以取到的是未知值，只不过编译器直接截拦掉这种取值行为，编译报错
